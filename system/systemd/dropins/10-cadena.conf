[Unit]
# Cadena de dependencias (bloque 1.4, punto 3).
# Wants= y no Requires=: el detector debe arrancar aunque el modo
# silente este deshabilitado, que es el caso durante el desarrollo.
After=guard-silent-mode.service
